/* ============================================================
   IGNEA LABS — Scoring Engine (deprecated)
   The intake form does not use scoring. This file is kept
   as a no-op to avoid 404s from any pages that still load it.
   ============================================================ */
var IgneaScoring = { calculate: function() { return {}; }, getRecommendations: function() { return []; }, calculateROI: function() { return {}; } };
